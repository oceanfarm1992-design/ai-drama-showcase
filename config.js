// Public config only — no secrets here. manifest.json is committed straight into
// this repo by the pipeline's publish step, so it's served same-origin by
// GitHub Pages alongside this file — no external storage needed.
const MANIFEST_URL = "manifest.json";
